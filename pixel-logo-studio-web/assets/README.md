# Pixel Logo Studio Assets

This folder intentionally left light-weight. All assets are generated on the fly by the app; no external downloads are required. The entire application works offline when served from any static host or opened directly via `index.html`.
